# MotionRecorder
MotionRecorder is a simple iOS application that collects motion (accelerometer) data in the background, stores it locally on the device, and allows the user to export recorded data as a CSV file
